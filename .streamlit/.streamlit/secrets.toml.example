# Streamlit Secrets 範例檔案
   # ⚠️ 這是範例！請勿上傳真實的 API Key 到 GitHub

   # 使用方式：
   # 1. 在 Streamlit Cloud → Settings → Secrets
   # 2. 複製以下內容並填入真實的 API Key

   GOOGLE_API_KEY = "your-api-key-here"
   GEMINI_MODEL = "gemini-3-flash-preview"
```

4. **Commit 訊息**：
```
   Add Secrets example file
